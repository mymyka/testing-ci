name: "📦 Dependency CVEs"

on:
  workflow_call:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  dependency-cves:
    name: "📦 Dependency CVEs"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "🔍 Running pip-audit ..."
      - run: echo "✅ No known CVEs in dependencies"
